# Supabase project used for storing the profile image and resume PDF.
# Leave unset to keep using the bundled local assets (src/assets/profile, public/resume.pdf).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Password required to access the hidden /admin page (upload profile image + resume).
VITE_ADMIN_PASSWORD=
